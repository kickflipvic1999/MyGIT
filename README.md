# Git Commands Test Repository

This repository is for testing and learning basic Git commands like:
- `git init`
- `git add .`
- `git commit -m "message"`
- `git push origin main`

## Purpose
Practice version control workflows without affecting real projects.

## Usage
1. Clone this repo
2. Make changes
3. Commit and push to see history on GitHub
